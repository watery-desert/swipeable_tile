name: swipeable_tile
description: A package to dismiss with animated background. Also Swipe to trigger an action without dismiss.
version: 1.0.0+6
repository: https://github.com/watery-desert/swipeable_tile


environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
