name: swipeable_tile
description: A package to dismiss with animated background. Also Swipe to tigger action without dismiss.
version: 2.0.0+3
repository: https://github.com/watery-desert/swipeable_tile


environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
